"""
W9｜GitHub + Zeabur 部署 · 复习模板
"""

# ============================================================
# 第1部分：本周核心概念
# ============================================================
"""
1. GitHub 建仓：新建仓库 → git init → git remote add → git push -u origin main。
2. PAT（Personal Access Token）存钥匙串：避免明文泄露 token；Linux 用 keyring，CI 用 Secrets。
3. .gitignore：排除 venv/、__pycache__/、.env、*.pyc 等，防止提交无用/敏感文件。
4. Zeabur 部署：将 GitHub 仓库直连 Zeabur，自动检测 Python 项目并配置运行环境。
5. requirements 锁版本：pip freeze > requirements.txt，确保生产环境与开发依赖完全一致。
"""

# ============================================================
# 第2部分：部署检查清单（填空）
# ============================================================
# 按顺序检查以下每一项，确保全部完成后才能点"部署"

DEPLOYMENT_CHECKLIST = {
    # --- 仓库信息 ---
    "仓库名（Repository Name）": "________",  # 填入你的 GitHub 仓库名，如 my-streamlit-app
    "分支名（Branch）": "________",          # 通常是 main 或 master

    # --- 环境变量（Zeabur → Settings → Environment Variables）---
    "环境变量1-变量名": "________",           # 如 DEEPSEEK_API_KEY
    "环境变量1-值": "________",              # 如 sk-xxxxx（不要明文写在代码里！）
    "环境变量2-变量名": "________",           # 如 OPENAI_API_KEY（如有）
    "环境变量2-值": "________",

    # --- requirements.txt 内容（必须包含所有依赖）---
    "requirements.txt 示例内容": """
streamlit>=1.30.0
openai>=1.10.0
chromadb>=0.4.0
beautifulsoup4>=4.12.0
# 锁定版本示例（推荐）：
# openai==1.12.0
# streamlit==1.35.0
""",
    # --- 入口文件 ---
    "启动命令（Run Command）": "streamlit run app.py --server.port $PORT --server.address 0.0.0.0",
    # 或（如果 Zeabur 自动检测）：
    "自动检测命令": "streamlit run app.py",
}

# ============================================================
# 第3部分：改造题
# ============================================================
# 【改造题1】将仓库名改为 "my-rag-assistant"，重新填写检查清单，并说明 .gitignore 需要添加的新条目。

# 【改造题2】如果需要添加 OpenAI API Key 作为第二个环境变量，
#   在以下 .env 文件中写出正确格式，并说明为什么不要把 .env 提交到 GitHub：
#   （答案写在下方注释中）
#
#   .env 文件内容：
#   ___________
#
#   为什么不能提交 .env：
#   ___________________________________________

# 【改造题3】requirements.txt 中有一行写的是 streamlit>=1.30.0，
#   请问 >= 和 == 有什么区别？在生产部署场景下推荐用哪个？为什么？


# ============================================================
# 第4部分：复习题（无骨架）
# ============================================================
"""
概念题：
1. 什么是 .gitignore？它的工作原理是什么？请列举 Python 项目至少3个必填的忽略规则。
2. PAT（Personal Access Token）和普通密码有什么区别？为什么推荐用 PAT 而非密码 push 代码？
3. Zeabur 部署时，"环境变量"和"启动命令"分别起什么作用？请说明两者在代码中的使用方式。

操作题：
4. 请按顺序写出从 git init 到首次 git push 的完整命令（假设远程已有空仓库）。
5. 部署后发现页面报 500 错误，请列出至少3步排查思路。
"""

# ============================================================
# 参考答案（完成后自行对照）
# ============================================================
"""
答案2：PAT 可精细化授权范围（如只给 repo 权限），可设置过期时间，可随时撤销；
       密码无法精细控制，一旦泄露需改密码。PAT 可存钥匙串，密码不应长期存储。

答案3：>= 允许安装指定版本或更高版本（可能引入破坏性更新）；== 固定精确版本。
       生产部署推荐 == 或 pip freeze 锁定，因为 >= 可能导致"在我电脑能跑"但线上崩掉。

答案4：
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main

答案5：① 看 Zeabur 日志找具体报错信息；② 检查环境变量是否全部填写；③ 检查 requirements.txt
       是否包含所有依赖；④ 本地先跑一遍 streamlit run app.py 确认无误；⑤ 查看代码中是否有
       hardcoded 的路径或变量名。
"""
