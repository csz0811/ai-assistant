"""
W10-D7｜RAG 完整流程图（ASCII 艺术版 + Mermaid 双版本）
========================================================
本文件 = 把 W10 一周学的内容画成一张图，从「离线建库」到「在线问答」
全部串起来。零代码执行，只是 print 大段字符串。

为什么 D7 要画图：
  - 文字看完容易忘，图能一眼看全
  - ASCII 画贴在 .md 文件里，零依赖任何工具
  - Mermaid 版贴到 腾讯文档/Notion 都能渲染
"""

# ============================================================
# 第 1 部分：离线建库阶段（脚本化、可重跑）
# ============================================================
OFFLINE_FLOW = r"""
╔══════════════════════════════════════════════════════════════════╗
║           RAG 离线建库阶段（数据预处理，一次或定期重跑）             ║
╚══════════════════════════════════════════════════════════════════╝

  [PDF/Markdown/网页]                           数据源
        │
        ▼
  ┌─────────────┐
  │ ① 文档加载  │ ← W10-D3：fitz(pymupdf) 读 PDF
  └─────┬───────┘
        │ 纯文本
        ▼
  ┌─────────────┐
  │ ② 文本切块  │ ← W10-D4：chunk_size=200 overlap=30
  └─────┬───────┘   （中文长文建议 500-1000）
        │ chunks[] 字符串列表
        ▼
  ┌─────────────┐
  │ ③ 向量化    │ ← W10-D2：embedding 模型
  └─────┬───────┘   （中文推荐 BAAI/bge-small-zh）
        │ 384 维浮点向量
        ▼
  ┌─────────────────────────┐
  │ ④ 存入向量数据库         │ ← W10-D5：chroma.PersistentClient
  │  (chroma_db/xx.pdf)      │
  │  - documents: 文本       │
  │  - metadatas: {来源,编号}│
  │  - ids: chunk_0, chunk_1│
  └─────────────────────────┘
        │
        ▼
  持久化到磁盘（重启后能复用）
"""

# ============================================================
# 第 2 部分：在线检索阶段（用户每次提问都跑）
# ============================================================
ONLINE_FLOW = r"""
╔══════════════════════════════════════════════════════════════════╗
║          RAG 在线检索阶段（用户每次提问都跑）                       ║
╚══════════════════════════════════════════════════════════════════╝

  用户问：「直播带货的复盘指标有哪些？」
        │
        ▼
  ┌──────────────┐
  │ ① 问题向量化 │ ← 同一个 embedding 模型
  └─────┬────────┘
        │ 1 个 384 维向量
        ▼
  ┌──────────────────────────┐
  │ ② 向量库相似度搜索        │ ← collection.query()
  │  - 算 L2/cosine 距离      │    默认 L2，越小越相似
  │  - 按距离排序             │
  │  - 取 top-k（通常 3-5）   │
  └─────┬────────────────────┘
        │ 候选块 + 距离
        ▼
  ┌──────────────────────────┐
  │ ③ 距离过滤（质量门）      │ ← W10-D6 知识整合 ⑤
  │  - 距离 < 1.0  → 保留     │   "距离"反映相对相似度
  │  - 距离 > 1.3  → 丢弃     │   样本少时会"凑数"
  └─────┬────────────────────┘
        │ 留下来的高相关块
        ▼
  ┌──────────────────────────┐
  │ ④ metadata 二次过滤（可选）│
  │  where={"来源": "xx.pdf"}│ ← 缩小检索范围
  └─────┬────────────────────┘
        │
        ▼
  拼成 Prompt，喂给 LLM 生成答案  ← W11 要学的
"""

# ============================================================
# 第 3 部分：W10 → W11 衔接（W11 = 在④后面接 LLM）
# ============================================================
W11_HOOK = r"""
╔══════════════════════════════════════════════════════════════════╗
║            W11 预告：在第②步后面挂上 LLM                          ║
╚══════════════════════════════════════════════════════════════════╝

  [D5+D6 的检索]
        │
        │  top_k 个块
        ▼
  ┌────────────────────────────────────────────┐
  │  拼 Prompt：                                │
  │  ┌──────────────────────────────────────┐  │
  │  │ 基于以下资料回答问题（禁止编造）：      │  │
  │  │                                       │  │
  │  │ [1] 复盘的六大指标：场观、停留时长、   │  │
  │  │     转化率、客单价、GMV、退货率。      │  │
  │  │ [2] 场观是基础指标，反映流量规模；     │  │
  │  │     停留时长反映内容吸引力。           │  │
  │  │                                       │  │
  │  │ 问题：直播带货的复盘指标有哪些？        │  │
  │  └──────────────────────────────────────┘  │
  └─────────────────┬──────────────────────────┘
                    │
                    ▼
              openai SDK（v3.0.0）
                    │
                    ▼
              DeepSeek API
                    │
                    ▼
              返回结构化答案
"""

# ============================================================
# 主程序：把所有流程图打印出来
# ============================================================
def print_section(title, content):
    print("\n" + "=" * 70)
    print(f"  {title}")
    print("=" * 70)
    print(content)

if __name__ == "__main__":
    print_section("【第一部分】离线建库（W10-D3~D5）", OFFLINE_FLOW)
    print_section("【第二部分】在线检索（W10-D5~D6）", ONLINE_FLOW)
    print_section("【第三部分】W11 预告：检索 + LLM", W11_HOOK)

    # ============================================================
    # Mermaid 版（贴到腾讯文档/Notion 都能渲染）
    # ============================================================
    MERMAID = """
graph TD
    A[📄 PDF/文档] --> B[① 文档加载<br/>fitz.get_text]
    B --> C[② 文本切块<br/>chunk_size=200]
    C --> D[③ 向量化<br/>embedding 模型]
    D --> E[④ 存入 ChromaDB<br/>persistent]

    F[❓ 用户问题] --> G[① 问题向量化]
    E --> H[② 向量库搜索<br/>算 L2 距离]
    G --> H
    H --> I[③ 距离过滤<br/>距离 < 1.0 保留]
    I --> J[④ metadata 过滤<br/>where 条件]
    J --> K[top-k 候选块]

    K -.W11.-> L[⑤ 拼 Prompt]
    L --> M[⑥ DeepSeek API]
    M --> N[✅ 返回结构化答案]

    style A fill:#e1f5ff
    style F fill:#fff4e1
    style N fill:#d4f4dd
    style L fill:#ffe1e1
    style M fill:#ffe1e1
"""
    print_section("【附】Mermaid 版（可贴到腾讯文档渲染）", MERMAID)
    print("\n💡 复制上面的 Mermaid 代码 → 打开 mermaid.live → 立即看到流程图\n")
